import React, { Component } from 'react';
import SearchBar from "./SearchBar";

export default (props)=> {
    return (
        <div>
            <SearchBar/>
            <p>Search Section</p>
        </div>
    );
};